version: "2"
services:
  kafka:
    image: centos:7
    ports:
    - "2181:2181"
    - "9092:9092"

    command: ['/docker-cmd']

registries:
  docker:
    url: https://hub.docker.com/
    namespace: jmalacho

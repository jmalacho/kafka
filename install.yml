---
- hosts: localhost
  tasks:
  - yum:
     state: present
     name: "{{item}}"
    with_items:
    - daemonize
    - java
    - "./{{dist}}/kafka-{{kafka_version}}.{{arch}}.rpm"

  - command: yum clean all

  - command: cp ./docker-cmd /

